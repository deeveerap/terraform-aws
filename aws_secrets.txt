resource "aws_secretsmanager_secret" "example" {
  name = "datacenter-secret"
}

variable "cred_vals" {
  default = {
    admin = "Namin123"
  }
  type = map(string)
}

resource "aws_secretsmanager_secret_version" "datacenter_secret_v" {
  secret_id     = aws_secretsmanager_secret.example.id
  secret_string = jsonencode(var.cred_vals)
}
